Поместите сюда доверенные веса `yolov8n.pt` (YOLOv8 Nano COCO detection).
Установите зависимости: `uv sync --extra ml`. Устройство задаётся через
`ML_DEVICE=gpu` (по умолчанию, CUDA) или `ML_DEVICE=cpu`.
Файлы весов не включены; загрузки весов из интернета при запуске нет.
